{
  "name": "classseats-help",
  "private": true,
  "devDependencies": {
    "pagefind": "^1.4.0"
  },
  "scripts": {
    "build": "pagefind --site public",
    "build:search": "pagefind --site public"
  }
}
